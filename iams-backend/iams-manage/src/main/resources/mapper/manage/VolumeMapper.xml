<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.iams.manage.mapper.VolumeMapper">
    
    <resultMap type="Volume" id="VolumeResult">
        <result property="id"    column="id"    />
        <result property="name"    column="name"    />
        <result property="volumeNumber"    column="volume_number"    />
        <result property="totalArchives"    column="total_archives"    />
        <result property="currentArchives"    column="current_archives"    />
        <result property="description"    column="description"    />
        <result property="status"    column="status"    />
        <result property="createTime"    column="create_time"    />
        <result property="createBy"    column="create_by"    />
        <result property="updateTime"    column="update_time"    />
        <result property="updateBy"    column="update_by"    />
    </resultMap>

    <sql id="selectVolumeVo">
        select id, name, volume_number, total_archives, current_archives, description, status, create_time, create_by, update_time, update_by from tb_volume
    </sql>

    <select id="selectVolumeList" parameterType="Volume" resultMap="VolumeResult">
        <include refid="selectVolumeVo"/>
        <where>  
            <if test="name != null  and name != ''"> and name like concat('%', #{name}, '%')</if>
            <if test="volumeNumber != null  and volumeNumber != ''"> and volume_number = #{volumeNumber}</if>
        </where>
    </select>
    
    <select id="selectVolumeById" parameterType="Long" resultMap="VolumeResult">
        <include refid="selectVolumeVo"/>
        where id = #{id}
    </select>

    <insert id="insertVolume" parameterType="Volume" useGeneratedKeys="true" keyProperty="id">
        insert into tb_volume
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="name != null and name != ''">name,</if>
            <if test="volumeNumber != null and volumeNumber != ''">volume_number,</if>
            <if test="totalArchives != null">total_archives,</if>
            <if test="currentArchives != null">current_archives,</if>
            <if test="description != null">description,</if>
            <if test="status != null and status != ''">status,</if>
            <if test="createTime != null">create_time,</if>
            <if test="createBy != null">create_by,</if>
            <if test="updateTime != null">update_time,</if>
            <if test="updateBy != null">update_by,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="name != null and name != ''">#{name},</if>
            <if test="volumeNumber != null and volumeNumber != ''">#{volumeNumber},</if>
            <if test="totalArchives != null">#{totalArchives},</if>
            <if test="currentArchives != null">#{currentArchives},</if>
            <if test="description != null">#{description},</if>
            <if test="status != null and status != ''">#{status},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="createBy != null">#{createBy},</if>
            <if test="updateTime != null">#{updateTime},</if>
            <if test="updateBy != null">#{updateBy},</if>
         </trim>
    </insert>

    <update id="updateVolume" parameterType="Volume">
        update tb_volume
        <trim prefix="SET" suffixOverrides=",">
            <if test="name != null and name != ''">name = #{name},</if>
            <if test="volumeNumber != null and volumeNumber != ''">volume_number = #{volumeNumber},</if>
            <if test="totalArchives != null">total_archives = #{totalArchives},</if>
            <if test="currentArchives != null">current_archives = #{currentArchives},</if>
            <if test="description != null">description = #{description},</if>
            <if test="status != null and status != ''">status = #{status},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
            <if test="createBy != null">create_by = #{createBy},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
        </trim>
        where id = #{id}
    </update>

    <delete id="deleteVolumeById" parameterType="Long">
        delete from tb_volume where id = #{id}
    </delete>

    <delete id="deleteVolumeByIds" parameterType="String">
        delete from tb_volume where id in 
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>